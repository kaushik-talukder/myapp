import { CoronaReportAdapter } from './corona-report-adapter';

describe('CoronaReportAdapter', () => {
  it('should create an instance', () => {
    expect(new CoronaReportAdapter()).toBeTruthy();
  });
});
